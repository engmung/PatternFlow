---
description: Tag and document a new Patternflow release
---

Trigger the `release-version` skill. Ask the user for the new version number (e.g. v1.1.0) and a short summary of changes since the last release before proceeding.
