---
description: Update docs/BUILD.md after hardware or firmware changes
---

Detect what has changed in `hardware/` or `firmware/` since BUILD.md was last touched. Suggest specific BUILD.md edits. If schematic or BOM is affected, trigger the `update-bom` skill.
